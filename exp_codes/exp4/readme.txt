This folde contains the code necessary for fault tolerant locomotion in Section 2.4. Some of it are operated on robot-side, which may not work for other robots. g_cpg_gait_generator.py works on PC side, which could be executed to generate the gait patterns. 

